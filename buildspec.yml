version: 0.2

phases:
  install:
    commands:
  pre_build:
    commands:
      - npm install
      - npm install pm2
  build:
    commands:
      - pm2 start server.js
  post_build:
    commands: